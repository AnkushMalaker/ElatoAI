#pragma once

// Device-specific secrets for Chronicle firmware.
// Copy this file to secrets.h (gitignored) and fill in your values.

// WiFi the device joins (no captive portal needed when these are set)
#define WIFI_SSID      "your-wifi-ssid"
#define WIFI_PASS      "your-wifi-password"

// Chronicle backend reachable on the LAN (plain http/ws, no TLS)
#define CHRONICLE_HOST "192.168.0.110"
#define CHRONICLE_PORT 8000

// Login used for POST /auth/jwt/login
#define CHRONICLE_USER "admin@example.com"
#define CHRONICLE_PASS "your-password"

// Becomes backend client_id "<objectid_suffix>-elato"
#define DEVICE_NAME    "elato"
